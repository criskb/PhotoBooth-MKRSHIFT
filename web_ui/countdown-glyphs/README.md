# Countdown glyphs

Optional custom countdown SVGs for the booth overlay.

Drop files here with these exact names:

- `5.svg`
- `4.svg`
- `3.svg`
- `2.svg`
- `1.svg`

When a file exists, the countdown will render that SVG for the matching number.
If a file is missing, the app automatically falls back to the regular text number.
